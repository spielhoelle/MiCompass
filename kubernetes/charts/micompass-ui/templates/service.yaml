apiVersion: v1
kind: Service
metadata:
  name: {{ include "micompass-ui.fullname" . }}
  labels:
    {{- include "micompass-ui.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "micompass-ui.selectorLabels" . | nindent 4 }}
